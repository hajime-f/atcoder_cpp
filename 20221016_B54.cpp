#include <iostream>
#include <map>
using namespace std;

int main() {
  
  int N;
  cin >> N;

  long A[100009];
  for (int i = 1; i <= N; i++)  cin >> A[i];

  
  
  
  return 0;
}

// cin >> N >> X;
// for (int i = 1; i <= N; i++)  cin >> A[i];

// int pos = lower_bound(A + 1, A + (N + 1), X[j]) - A;  // 二分探索

// sort(A + 1, A + (N + 1));  // ソート
