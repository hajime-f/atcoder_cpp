#include <iostream>
using namespace std;

int main() {


  return 0;
}

// cin >> N >> X;
// for (int i = 1; i <= N; i++)  cin >> A[i];

// int pos = lower_bound(A + 1, A + (N + 1), X[j]) - A;

// sort(A + 1, A + (N + 1));

